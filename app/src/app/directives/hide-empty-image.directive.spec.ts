import { HideEmptyImageDirective } from './hide-empty-image.directive';

describe('HideEmptyImageDirective', () => {
  it('should create an instance', () => {
    const directive = new HideEmptyImageDirective();
    expect(directive).toBeTruthy();
  });
});
